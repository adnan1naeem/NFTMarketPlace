import { Typography } from "@mui/material";
import { styled } from "@mui/system";
import React from "react";
export const Flex = styled(Typography)(({ theme }) => ({
  display: "flex",
  alignItems: "center",
}));
