import { Typography } from "@mui/material";
import { styled } from "@mui/system";
import React from "react";
export const ManropeText = styled(Typography)(({ theme }) => ({
  fontFamily: "Manrope",
  color: "#475569",
  textAlign: "left",
  fontWeight: 500,
  fontSize: "16px",
}));
